<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GoMind" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Установка цвета фона приложения -->
        <item name="colorPrimary">@color/soft_blue</item>
        <item name="colorPrimaryVariant">@color/soft_orange</item>
        <item name="bottomNavigationStyle">@style/MyBottomNavigationView</item>
        <item name="buttonStyle">@style/CustomButton</item>
    </style>

    <style name="CustomButton" parent="Widget.MaterialComponents.Button">
        <!-- Отключаем материал-фон -->
        <item name="android:background">@drawable/rounded_button</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="MyBottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemIconTint">@null</item>
        <item name="itemTextColor">@null</item>
    </style>

    <style name="Theme.GoMind" parent="Base.Theme.GoMind" />
</resources>