<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black">

    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="-10dp"
        android:background="@drawable/message"
        android:padding="30dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/support_text"
            android:layout_gravity="center_horizontal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Вы точно хотите выйти из аккаунта?"
            android:fontFamily="@font/regular"
            android:textStyle="bold"
            android:textSize="20sp"
            android:textColor="#000000"
            android:textAlignment="center"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginTop="20dp">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/byu_fruits"
                android:layout_height="48dp"
                android:layout_width="100dp"
                android:layout_marginEnd="20dp"
                android:text="Да"
                android:background="@drawable/noclick2"
                app:iconTintMode="multiply"
                app:backgroundTint="@null"
                android:fontFamily="@font/regular"
                android:textStyle="bold"
                android:textSize="20sp"
                android:textAlignment="center"
                android:textFontWeight="900"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/add"
                android:layout_height="48dp"
                android:layout_width="100dp"
                android:text="Нет"
                android:background="@drawable/noclick"
                app:iconTintMode="multiply"
                app:backgroundTint="@null"
                android:fontFamily="@font/regular"
                android:textStyle="bold"
                android:textSize="20sp"
                android:textAlignment="center"
                android:textFontWeight="900"/>

        </LinearLayout>

    </LinearLayout>

    <!-- Круг с крестиком внизу страницы -->
    <FrameLayout
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="30dp">

        <ImageView
            android:id="@+id/cross_circle"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/circle_background" />

        <ImageView
            android:id="@+id/cross_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/cllose"
            android:layout_gravity="center"/>
    </FrameLayout>

</RelativeLayout>
